/**
 * Prisma client singleton.
 *
 * To use Prisma:
 *   1. `npm install @prisma/client`
 *   2. `npx prisma init` – generates prisma/schema.prisma
 *   3. Define your models in schema.prisma
 *   4. `npx prisma migrate dev` – applies migrations
 *   5. Uncomment the import below
 *
 * For local development without Prisma, the in-memory repos in this folder
 * provide a working substitute.
 */

// import { PrismaClient } from '@prisma/client';
// export const prisma = new PrismaClient();

export const prisma = null; // placeholder until Prisma is configured
